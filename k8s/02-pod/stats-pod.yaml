# Part 2 — Pod (stats-service)
#
# Fill in every ??? and apply alongside api-pod.yaml:
#   kubectl apply -f k8s/02-pod/stats-pod.yaml
#
# Verify:
#   kubectl get pods
#   kubectl logs stats-service
#
# NOTE: You cannot reach this pod from your browser yet — it has no Service.
# That is intentional. You will create its Service in Part 3.

apiVersion: v1
kind: Pod
metadata:
  name: stats-service
  labels:
    app: ???
spec:
  containers:
    - name: stats-service
      image: ???    # docker build -t stats-service:latest ./stats-service
      imagePullPolicy: Never
      ports:
        - containerPort: ???   # Check stats-service/src/index.js.
      env:
        - name: DATABASE_URL
          value: "postgresql://postgres:secret@postgres:5432/notesdb"
        - name: PORT
          value: "4000"
